{
  "name": "lambda",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "compile": "tsc",
    "build:lite": "npm run compile && cp ./package.json ./dist/package.json && cd ./dist && npm i --omit=dev && zip -r finalAssignment.zip ."
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/jsonwebtoken": "^9.0.6"
  },
  "dependencies": {
    "ajv": "^8.16.0",
    "dotenv": "^16.4.5",
    "jsonwebtoken": "^9.0.2"
  }
}
